// Test file: test.deca

{
    // Variable declarations with different types and initializations
    int x = 5;
    int y = 3;
    float f = 2.5;
    float floatResult = x * f;
    int sum = x + y;
    int product = x * y;


    
    // Arithmetic and type conversion
    float result = x + f;
    
    // Print statements
    println("=== Test Program ===");
    print("x = "); println(x);
    print("y = "); println(y);
    print("f = "); println(f);
    print("x + f = "); println(result);
    
    // Conditional test
    if (x > y) {
        println("x is greater than y");
    } else {
        println("x is less than or equal to y");
    }
    
    // Integer arithmetic
    print("Sum = "); println(sum);
    print("Product = "); println(product);
    
    // Float arithmetic and conversion
    print("x * f = "); println(floatResult);
    
    // Boolean operations
    if (x > 0 && y < 10) {
        println("Both conditions are true");
    }
    
    println("=== Test Complete ===");
}
