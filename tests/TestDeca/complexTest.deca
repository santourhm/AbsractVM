{
    // Variable declarations with initialization
    int x = 10;
    int y = 5;
    boolean isValid = true;
    float result = 0.0;
    boolean isXEven;
    boolean isYEven;
    // Test arithmetic operations first
    result = x + y;
    println("Addition: ", result);
    
    result = x - y;
    println("Subtraction: ", result);
    
    result = x * y;
    println("Multiplication: ", result);
    
    result = x / y;
    println("Division: ", result);

    // Test while loop with compound conditions
    while (x >= 0 && y >= 0) {
        // Evaluate conditions separately for clarity
        isXEven = x % 2 == 0;
        isYEven = y % 2 == 0;
        
        if (isXEven || isYEven) {
            println("Either x or y is even");
            result = result + 0.5;
        } else {
            println("Both x and y are odd");
            result = result - 0.5;
        }
        
        // Update counters after evaluation
        x = x - 1;
        y = y - 1;
        println("Current values: x = ", x, ", y = ", y);
    }

    println("Final result: ", result);
    println("Test completed");
}
